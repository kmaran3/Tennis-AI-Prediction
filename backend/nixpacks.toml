[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "echo '=== /app contents ===' && ls -la /app/ && echo '=== /app/data ===' && ls -la /app/data/ 2>/dev/null || echo 'NO DATA DIR' && uvicorn main:app --host 0.0.0.0 --port $PORT"
